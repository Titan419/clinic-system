services:
  - type: web
    name: clinic-system
    env: php
    plan: free
    buildCommand: "php -v"
    startCommand: "php -S 0.0.0.0:$PORT -t ."
    envVars:
      - key: PHP_VERSION
        value: 8.2
      - key: DATABASE_URL
        sync: false
